# Полное руководство по проекту CraftArmor 3.0 (EverShop)

## 📋 Содержание

1. [Структура проекта](#структура-проекта)
2. [Конфигурация](#конфигурация)
3. [Темы (Themes)](#темы-themes)
4. [Расширения (Extensions)](#расширения-extensions)
5. [API маршруты](#api-маршруты)
6. [Команды](#команды)
7. [Важные моменты](#важные-моменты)

---

## 📁 Структура проекта

```
craftarmor_3_0/
├── config/                    # Конфигурационные файлы
│   ├── default.json           # Базовая конфигурация (в Git)
│   ├── development.json       # Конфигурация для разработки (в Git)
│   └── local.json.example     # Пример локальной конфигурации
├── extensions/                # Кастомные расширения
│   └── test-api/              # Пример расширения
│       ├── src/
│       │   ├── api/           # API маршруты
│       │   └── bootstrap.ts    # Инициализация расширения
│       ├── dist/               # Скомпилированный код (автоматически)
│       ├── package.json
│       └── tsconfig.json
├── themes/                    # Кастомные темы
│   └── craftarmor/            # Наша тема
│       ├── src/
│       │   ├── components/    # Общие компоненты
│       │   └── pages/         # Компоненты для страниц
│       ├── dist/              # Скомпилированный код (автоматически)
│       ├── public/            # Публичные ресурсы (изображения, шрифты)
│       ├── package.json
│       └── tsconfig.json
├── public/                    # Публичные файлы
├── media/                     # Загруженные медиа-файлы
├── .evershop/                 # Служебные файлы EverShop (создается после setup)
├── package.json               # Корневой package.json с workspaces
└── .gitignore
```

---

## ⚙️ Конфигурация

### Система конфигурации EverShop

EverShop использует `node-config` с приоритетом загрузки (от низкого к высокому):

1. `config/default.json` - базовая конфигурация (в Git)
2. `config/[NODE_ENV].json` - конфигурация для окружения (development/production)
3. `config/local.json` - локальные переопределения (НЕ в Git)
4. **Переменные окружения** - наивысший приоритет

### Основные настройки

**config/default.json:**
```json
{
  "shop": {
    "name": "CraftArmor",
    "language": "ru",
    "currency": "RUB",
    "timezone": "UTC",
    "homeUrl": "http://localhost:3000"
  },
  "system": {
    "theme": "craftarmor",
    "extensions": [
      {
        "name": "test-api",
        "resolve": "extensions/test-api",
        "enabled": true,
        "priority": 10
      }
    ]
  }
}
```

### config/local.json vs .env

**config/local.json** (система node-config):
- JSON формат, структурированный
- Переопределяет настройки из default.json
- Удобен для локальной разработки

**.env** (переменные окружения):
- Простой формат key=value
- Наивысший приоритет
- Удобен для production и секретов

**Рекомендация:** Используйте оба:
- `config/local.json` - для структурированных настроек
- `.env` - для секретов (пароли, ключи API)

---

## 🎨 Темы (Themes)

### Где находится стандартная тема?

**Стандартная тема "default"** не имеет отдельной папки - она распределена по модулям:
```
node_modules/@evershop/evershop/dist/modules/[moduleName]/pages/
```

Каждый модуль содержит свои компоненты UI, которые вместе образуют стандартную тему.

### Структура кастомной темы

```
themes/craftarmor/
├── src/
│   ├── components/          # Общие компоненты
│   │   └── common/
│   └── pages/               # Компоненты для страниц
│       └── all/             # Компоненты для всех страниц
│           └── All.tsx      # Главный компонент (опционально)
├── public/                  # Публичные ресурсы
│   └── images/
├── dist/                    # Скомпилированный код (автоматически)
├── package.json
└── tsconfig.json
```

### package.json темы

```json
{
  "name": "craftarmor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile": "tsc"
  },
  "dependencies": {
    "@evershop/evershop": "^2.1.0"
  }
}
```

### tsconfig.json темы

```json
{
  "compilerOptions": {
    "module": "NodeNext",
    "target": "ES2018",
    "outDir": "./dist",
    "rootDir": "src",
    "jsx": "react"
  },
  "include": ["src"]
}
```

### Как работает переопределение тем

**Каскадная система:**
1. Сначала ищет компонент в теме `craftarmor` (themes/craftarmor/)
2. Если не найден - использует компонент из стандартной темы `default` (встроена в модули)

**Это означает:** Мы переопределяем только нужные компоненты, остальные автоматически берутся из стандартной темы!

### Активация темы

В `config/default.json`:
```json
{
  "system": {
    "theme": "craftarmor"
  }
}
```

**Важно:** После изменения темы нужно запустить `npm run build` для применения изменений.

### Компонент All.tsx

**Опционально!** Если не создавать `All.tsx`, будет использоваться стандартный компонент из темы "default".

Если создаете `All.tsx`, он должен включать все необходимые области (header, footer, body и т.д.), иначе будет белый экран.

**Рекомендация:** Не создавать `All.tsx`, если не нужна полная кастомизация. Переопределять только конкретные компоненты по мере необходимости.

### Папка dist для темы

- **В dev режиме** (`npm run dev`) - компилируется автоматически из `src/`, папка `dist/` не обязательна
- **В production** (`npm run build`) - требуется наличие папки `dist/` со скомпилированными файлами
- **Компиляция:** Темы компилируются автоматически EverShop, ручная компиляция не требуется в dev режиме

Если в теме нет компонентов (пустая папка `src/`), можно создать пустую папку `dist/` вручную.

---

## 🔌 Расширения (Extensions)

### Структура расширения

```
extensions/test-api/
├── src/
│   ├── api/                  # API маршруты
│   │   └── test/
│   │       ├── route.json
│   │       └── getTestMiddleware.ts
│   ├── pages/                # Frontend страницы (опционально)
│   │   ├── admin/            # Админ панель
│   │   └── frontStore/       # Storefront
│   ├── migration/            # Миграции БД (ВАЖНО: в src/migration/)
│   │   └── Version-1.0.0.ts  # Формат: Version-X.Y.Z.ts
│   ├── services/             # Сервисы (опционально)
│   ├── jobs/                 # Cron jobs (опционально)
│   └── bootstrap.ts          # Инициализация расширения
├── dist/                     # Скомпилированный код
│   ├── migration/            # Скомпилированные миграции (обязательно!)
│   │   └── Version-1.0.0.js
│   └── ...                   # Остальные файлы
├── package.json
└── tsconfig.json
```

**Важно о миграциях:**
- Миграции должны быть в `src/migration/` (не `migrations`!)
- Имя файла: `Version-X.Y.Z.ts` (например, `Version-1.0.0.ts`)
- После компиляции должны быть в `dist/migration/Version-X.Y.Z.js`
- Миграции применяются автоматически при запуске сервера

### package.json расширения

```json
{
  "name": "test-api",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/bootstrap.js",
  "scripts": {
    "compile": "tsc"
  },
  "dependencies": {
    "@evershop/evershop": "^2.1.0"
  }
}
```

### tsconfig.json расширения

```json
{
  "compilerOptions": {
    "module": "NodeNext",
    "target": "ES2018",
    "outDir": "./dist",
    "rootDir": "src",
    "jsx": "react"
  },
  "include": ["src"]
}
```

### Активация расширения

В `config/default.json`:
```json
{
  "system": {
    "extensions": [
      {
        "name": "test-api",
        "resolve": "extensions/test-api",
        "enabled": true,
        "priority": 10
      }
    ]
  }
}
```

### Bootstrap файл

`src/bootstrap.ts` - выполняется при запуске приложения:
```typescript
export default function bootstrap() {
  console.log('Extension loaded successfully!');
}
```

### Миграции базы данных

**Структура:**
- Миграции находятся в `src/migration/` (единственное число!)
- Формат имени файла: `Version-X.Y.Z.ts` (например, `Version-1.0.0.ts`)
- После компиляции должны быть в `dist/migration/Version-X.Y.Z.js`

**Формат миграции:**
```typescript
import { PoolClient } from 'pg';

export default async function (connection: PoolClient) {
  await connection.query(`
    CREATE TABLE IF NOT EXISTS my_table (
      id SERIAL PRIMARY KEY,
      name VARCHAR(255) NOT NULL
    )
  `);
}
```

**Применение:**
- Миграции применяются автоматически при запуске сервера (`npm run dev` или `npm run start`)
- EverShop проверяет версию и применяет только новые миграции
- Информация о примененных миграциях хранится в таблице `migration` в БД

**Важно:**
- Миграции должны быть скомпилированы в `dist/migration/` перед запуском
- Для этого нужно запустить `npm run build` в папке расширения
- В dev режиме остальные файлы компилируются автоматически, но миграции требуют ручной компиляции

### Cron Jobs

**Регистрация cron job в bootstrap.ts:**
```typescript
import { registerJob } from '@evershop/evershop/lib/cronjob';
import syncData from './jobs/syncData.js';
import * as path from 'path';
import * as url from 'url';
import * as fs from 'fs';

export default async function bootstrap() {
  // Получаем путь к скомпилированному файлу задачи
  const __filename = url.fileURLToPath(import.meta.url);
  const __dirname = path.dirname(__filename);
  const jobPath = path.resolve(__dirname, 'jobs', 'syncData.js');
  
  // Проверяем, что файл существует
  if (!fs.existsSync(jobPath)) {
    console.warn(`Job file not found: ${jobPath}`);
    return;
  }
  
  // Регистрируем cron job
  registerJob({
    name: 'syncData',
    resolve: jobPath,
    schedule: '0 3 * * *', // Каждый день в 03:00
    enabled: true
  });
}
```

**Формат cron выражения:** Стандартный cron формат (например, `0 3 * * *` = каждый день в 03:00)

---

## 🌐 API маршруты

### Структура API маршрута

```
extensions/test-api/src/api/test/
├── route.json
└── getTestMiddleware.ts
```

### route.json

```json
{
  "methods": ["GET"],
  "path": "/test",
  "access": "public"
}
```

**Важно:**
- `methods` - массив, не строка! `["GET"]`, а не `"GET"`
- `path` - путь БЕЗ префикса `/api/` (EverShop добавляет автоматически)
- `access` - `"public"`, `"admin"`, или `"customer"`

### Именование middleware файлов

**Формат:** `[method]nameMiddleware.ts`

**Примеры:**
- `[get]testMiddleware.ts` - для GET запросов
- `[post]saveCommentMiddleware.ts` - для POST запросов
- `[put]updateProductMiddleware.ts` - для PUT запросов
- `[delete]removeItemMiddleware.ts` - для DELETE запросов

**Альтернатива:** Можно использовать простое имя без скобок:
- `getTestMiddleware.ts` (тоже работает)

### Откуда берется префикс `/api/`?

**От имени папки!** Если файл находится в папке `src/api/`, EverShop автоматически добавляет префикс `/api/` к пути из `route.json`.

**Пример:**
- Папка: `src/api/test/`
- route.json: `{"path": "/test"}`
- Итоговый URL: `http://localhost:3000/api/test` ✅

**Если указать полный путь:**
- route.json: `{"path": "/api/test"}`
- Итоговый URL: `http://localhost:3000/api/api/test` ❌ (двойной префикс!)

### Middleware файл

```typescript
import { Request, Response } from 'express';

export default async function getTestMiddleware(
  request: Request,
  response: Response
) {
  response.json({
    success: true,
    message: 'Test API endpoint is working!',
    data: {
      timestamp: new Date().toISOString()
    }
  });
}
```

---

## 🛠️ Команды

### Основные команды

```bash
# Установка зависимостей
npm install

# Настройка проекта (первый запуск)
npm run setup

# Сборка проекта
npm run build

# Запуск в режиме разработки (автоматическая компиляция)
npm run dev

# Запуск в production режиме
npm run start

# Запуск с отладкой
npm run start:debug

# Заполнение демо-данными (только для разработки!)
npm run seed:all

# Создание пользователя
npm run user:create

# Изменение пароля пользователя
npm run user:changePassword
```

### Компиляция расширения/темы

**Расширения:**

```bash
# Из папки расширения (для миграций обязательно!)
cd extensions/test-api
npm run build

# Или из корня (компилирует все)
npm run build
```

**Важно для расширений:**
- В dev режиме (`npm run dev`): EverShop компилирует файлы из `src/` на лету
- **НО:** Миграции должны быть скомпилированы в `dist/migration/` вручную!
- Для миграций нужно запустить `npm run build` в папке расширения перед запуском сервера
- В production: требуется полностью скомпилированная папка `dist/`

**Темы:**

```bash
# Темы компилируются автоматически EverShop
# Ручная компиляция не требуется в dev режиме
# В production компилируется при npm run build в корне
```

**Важно для тем:**
- В dev режиме: компилируется автоматически из `src/`, `dist/` не обязательна
- В production: компилируется автоматически при `npm run build` в корне проекта
- Ручная компиляция не требуется

---

## ⚠️ Важные моменты

### 1. Workspaces

В корневом `package.json` должны быть workspaces:
```json
{
  "workspaces": [
    "extensions/*",
    "themes/*"
  ]
}
```

Это позволяет npm управлять зависимостями для всех расширений и тем.

### 2. Папка dist

**Для расширений:**
- **В dev режиме** (`npm run dev`): 
  - Остальные файлы компилируются автоматически из `src/`
  - **НО:** Миграции должны быть в `dist/migration/` (требуется ручная компиляция!)
- **В production** (`npm run build`): требуется полностью скомпилированная папка `dist/`

**Для тем:**
- **В dev режиме** (`npm run dev`): компилируется автоматически из `src/`, `dist/` не обязательна
- **В production** (`npm run build`): компилируется автоматически при сборке проекта
- Если в теме нет компонентов, можно создать пустую папку `dist/` вручную

### 2.1. Структура компиляции

**Расширения:**
- `src/` → компилируется в → `dist/` (с `rootDir: "src"`)
- Миграции: `src/migration/Version-X.Y.Z.ts` → `dist/migration/Version-X.Y.Z.js`
- Остальные файлы: `src/api/` → `dist/api/`, `src/services/` → `dist/services/` и т.д.

**Темы:**
- `src/` → компилируется в → `dist/` (с `rootDir: "src"`)
- Компоненты: `src/components/` → `dist/components/`
- Страницы: `src/pages/` → `dist/pages/`

### 3. Конфигурация темы

После изменения темы в конфиге нужно запустить:
```bash
npm run build
```

### 4. API маршруты

- Путь в `route.json` должен быть БЕЗ префикса `/api/` (добавляется автоматически)
- `methods` - массив, не строка: `["GET"]`, а не `"GET"`
- Middleware файл должен экспортировать default функцию

### 5. Стандартная тема

- Стандартная тема "default" встроена в модули EverShop
- Не нужно копировать её структуру
- Можно переопределять только нужные компоненты
- Остальные компоненты автоматически берутся из стандартной темы

### 6. Расширения и темы

- Расширения добавляют функциональность (API, страницы, сервисы)
- Темы изменяют внешний вид (компоненты UI)
- Оба могут быть в workspaces и иметь свои зависимости

### 7. TypeScript и компиляция

**Расширения:**
- Все файлы в `src/` - TypeScript
- Компилируются в JavaScript в `dist/`
- В dev режиме: компиляция происходит автоматически для большинства файлов
- **Исключение:** Миграции должны быть скомпилированы вручную в `dist/migration/`
- В production: нужно компилировать перед запуском (`npm run build` в папке расширения)

**Темы:**
- Все файлы в `src/` - TypeScript
- Компилируются в JavaScript в `dist/`
- В dev режиме: компиляция происходит автоматически
- В production: компилируется автоматически при `npm run build` в корне проекта
- Ручная компиляция не требуется

**Настройка tsconfig.json:**
- `rootDir: "src"` - обязательно для правильной структуры
- `outDir: "./dist"` - папка для скомпилированных файлов
- `include: ["src"]` или `include: ["src/**/*"]` - что компилировать

### 8. Конфигурация

- `config/default.json` - базовая (в Git)
- `config/development.json` - для разработки (в Git)
- `config/local.json` - локальная (НЕ в Git, для секретов)
- Переменные окружения - наивысший приоритет

---

## 📚 Полезные ссылки

- [Документация EverShop](https://evershop.io/docs)
- [Extension Overview](https://evershop.io/docs/development/module/extension-overview)
- [Extension Development](https://evershop.io/docs/development/module/extension-development)
- [Create Your First Extension](https://evershop.io/docs/development/module/create-your-first-extension)
- [Theme Overview](https://evershop.io/docs/development/theme/theme-overview)
- [View System](https://evershop.io/docs/development/theme/view-system)
- [Configuration Guide](https://evershop.io/docs/development/knowledge-base/configuration-guide)

---

## 🎯 Быстрый старт

1. **Установка зависимостей:**
   ```bash
   npm install
   ```

2. **Настройка проекта:**
   ```bash
   npm run setup
   ```

3. **Запуск в режиме разработки:**
   ```bash
   npm run dev
   ```

4. **Доступ:**
   - Frontend: http://localhost:3000
   - Admin Panel: http://localhost:3000/admin
   - API: http://localhost:3000/api/test

---

*Последнее обновление: 2025-12-11*

