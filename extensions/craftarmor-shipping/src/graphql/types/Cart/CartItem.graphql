extend interface ShoppingCartItem {
  productLength: Float
  productWidth: Float
  productHeight: Float
}

extend type CartItem {
  isSelected: Boolean
  productLength: Float
  productWidth: Float
  productHeight: Float
}

extend type OrderItem {
  productLength: Float
  productWidth: Float
  productHeight: Float
}
